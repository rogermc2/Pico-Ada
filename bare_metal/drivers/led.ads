
package led is

   procedure Configure_Led (LED_PIN : Natural);
   procedure Turn_On_Led (LED_PIN : Natural);
   procedure Turn_Off_Led (LED_PIN : Natural);
   procedure Toggle_Led (LED_PIN : Natural);

end led;
